# Copy to .env for local `netlify dev`, and set the same var in the Netlify UI
# (Site settings -> Environment variables) for production.

# Free key from the U.S. Energy Information Administration:
#   https://www.eia.gov/opendata/register.php
# Without it, the crude feed falls back to a baseline value (the app still runs).
EIA_API_KEY=glNBpxGkPsutfhfv9UWivRTTMXQBQ2UmfGi8o1CD
